package starter.parametrized.valueSource;

public class NumberUtils {
    // Bir sayının çift olup olmadığını kontrol eden method
    public static boolean isEven(int number) {
        return number % 2 == 0;
    }
}
